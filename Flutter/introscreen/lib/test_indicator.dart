import 'package:flutter/material.dart';

class TestIndicator extends StatelessWidget {
  const TestIndicator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('hELLO')),
    );
  }
}
